import React, { useRef } from "react";
import { Canvas, useFrame, useThree } from "@react-three/fiber";

// 마우스를 따라다니는 3D Object 컴포넌트
const CursorEffectOverlay = () => {
  // mesh에 접근하기 위한 ref 생성
  const meshRef = useRef();

  // 캔버스, 카메라, 마우스 등 R3F의 상태에 접근

  return <></>;
};

export default CursorEffectOverlay;
